# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# ================================
# DATABASE CONFIGURATION
# ================================

# PostgreSQL Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_postgres_password_here
POSTGRES_DB=ads_tracker

# PostgreSQL Database Connection URL
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
# For local development (docker-compose postgres maps to host 5433): postgresql://postgres:postgres@localhost:5433/ads_tracker?schema=public
# For Docker Compose (production): postgresql://postgres:PASSWORD@postgres:5432/ads_tracker?schema=public
DATABASE_URL="postgresql://postgres:your_secure_postgres_password_here@postgres:5432/ads_tracker?schema=public"

# ================================
# APPLICATION CONFIGURATION
# ================================

# Node Environment (development, test, production)
NODE_ENV="production"

# Application URL (used for redirects, emails, etc.)
# For local development: http://localhost:3000
# For production: https://your-domain.com
NEXT_PUBLIC_APP_URL="https://ads.nugrohopramono.my.id"

# JWT Secret for authentication (generate a secure random string)
# You can generate one using: openssl rand -base64 32
JWT_SECRET="your_secure_jwt_secret_here"

# ================================
# META GRAPH API CONFIGURATION
# ================================
# Add your Meta/Facebook API credentials here if needed
# META_APP_ID=
# META_APP_SECRET=
# META_ACCESS_TOKEN=

# ================================
# AI AGENT CONFIGURATION
# ================================
# Add ONE of the following keys to enable the Data Analyst Agent
# The agent checks for keys in this order: Gemini > OpenRouter > OpenAI

# Option 1: Google Gemini (Get key: https://aistudio.google.com/app/apikey)
# GOOGLE_GENERATIVE_AI_API_KEY=

# Option 2: OpenRouter (Get key: https://openrouter.ai/keys)
# OPENROUTER_API_KEY=

# Option 3: OpenAI (Get key: https://platform.openai.com/api-keys)
# OPENAI_API_KEY=
